<%= link_to "Add a new place", "/places/new" %>

<p></p>

<ul>

  <% @places.each do |place| %>
    <li>
  	  <%= link_to place.title, "/places/#{place.id}" %> <%= link_to " (delete)", "/places/#{place.id}/delete" %>
    </li>
  <% end %>

</ul>
